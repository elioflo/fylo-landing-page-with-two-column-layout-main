import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://elioflo.github.io',
  base: '/fylo-landing-page-with-two-column-layout-main',
});
